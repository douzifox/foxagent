{
  "name": "foxagent",
  "description": "Standalone coding agent on any OpenAI-compatible model, plugged into Claude Code via MCP: a cheaper second worker with a clean context — code review by a different model, persistent resumable sessions, cost tiering. Bun + TypeScript.",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "cli": "bun src/cli.ts",
    "cli:compile": "bun build --compile src/cli.ts --outfile foxagent",
    "cli:compile:win": "bun build --compile --target=bun-windows-x64 src/cli.ts --outfile foxagent.exe"
  },
  "devDependencies": {
    "@types/diff": "^8.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "diff": "^9.0.0"
  }
}
